# Core dependencies
numpy>=1.21.0
matplotlib>=3.5.0
astropy>=5.0
cartopy>=0.21.0
Pillow>=9.0.0

# AI/ML components
langchain-core>=0.1.0
langchain-openai>=0.1.0
pydantic>=2.0.0

# LangGraph for agent construction
langgraph>=0.1.0

# MCP adapters for tool integration
langchain-mcp-adapters>=0.1.0

# Development extras (optional)
jupyter>=1.0.0
ipython>=8.0.0

# Installation notes:
# For cartopy, you may need to install system dependencies first:
# On Ubuntu/Debian: sudo apt-get install libgeos-dev proj-bin
# On MacOS: brew install geos proj
